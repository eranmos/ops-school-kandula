apiVersion: v1
kind: Pod
metadata:
  name: "{{ include "fluentd_cet.fullname" . }}-test-connection"
  labels:
    {{- include "fluentd_cet.labels" . | nindent 4 }}
  annotations:
    "helm.sh/hook": test-success
spec:
  containers:
    - name: wget
      image: busybox
      command: ['wget']
      args: ['{{ include "fluentd_cet.fullname" . }}:{{ .Values.service.port }}']
  restartPolicy: Never
